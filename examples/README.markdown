# Weer Usage Examples

1. `npm install`
2.
   - `npm run without-bundlers`
   - `npm run webpack`
   - `npm run rollup `
3.
   - Chrome: load unpacked extension from `./dist`
   - Firefox: `cd dist && web-ext run`
